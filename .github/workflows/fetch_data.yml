name: 股票數據抓取

on:
  # 周日到周五 早上九點到下午一點，每30分鐘執行一次（台灣時間）
  schedule:
    - cron: '0,30 1-5 * * 0-5'   # UTC 時間，對應台灣時間 09:00-13:30
  
  # 允許手動觸發
  workflow_dispatch:
  
  # 推送到 main 分支時也執行（用於測試）
  push:
    branches:
      - main
    paths:
      - 'fetch_stock_data.py'
      - '.github/workflows/fetch_data.yml'

jobs:
  fetch-data:
    runs-on: ubuntu-latest
    
    steps:
    - name: 檢出代碼
      uses: actions/checkout@v4
      with:
        fetch-depth: 0  # 需要完整歷史以便推送
    
    - name: 設置 Python 環境
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: 安裝依賴
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: 執行數據抓取
      run: |
        echo "開始抓取股票數據..."
        python fetch_stock_data.py
    
    - name: 配置 Git
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "GitHub Actions Bot"
    
    - name: 提交並推送數據
      run: |
        git add data/
        if git diff --staged --quiet; then
          echo "沒有變更需要提交"
        else
          git commit -m "📊 更新股票數據 $(date +'%Y-%m-%d %H:%M:%S')"
          git push
        fi
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    
    - name: 顯示執行結果
      run: |
        echo "✓ 數據抓取完成"
        ls -lh data/
        echo "最新數據:"
        cat data/latest.json | head -20
