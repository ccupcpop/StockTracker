# GitHub 部署指南

## 步驟 1: 建立 GitHub Repository

1. 登入 GitHub 帳號
2. 點擊右上角 "+" → "New repository"
3. 設定 Repository:
   - Repository name: `taiwan-stock-tracker` (或任何你喜歡的名稱)
   - Description: `台股新聞熱門股票追蹤系統`
   - Public 或 Private (建議 Public 才能使用 GitHub Pages)
   - 勾選 "Add a README file"
4. 點擊 "Create repository"

## 步驟 2: 上傳檔案到 Repository

### 方法 A: 使用網頁介面上傳

1. 在 Repository 頁面,點擊 "Add file" → "Upload files"
2. 上傳以下檔案:
   - `stock_analysis.py`
   - `requirements.txt`
   - `.gitignore`
   - `README.md`
   - `index.html`
3. 創建 `.github/workflows` 資料夾並上傳 `stock_analysis.yml`
4. Commit changes

### 方法 B: 使用 Git 指令

```bash
# 下載 Repository
git clone https://github.com/你的帳號/taiwan-stock-tracker.git
cd taiwan-stock-tracker

# 複製所有檔案到這個資料夾
# (把下載的檔案放進來)

# 提交到 GitHub
git add .
git commit -m "Initial commit: Taiwan stock tracker system"
git push origin main
```

## 步驟 3: 準備資料檔案

在 Repository 中建立 `StockInfo` 資料夾,並上傳以下檔案:

### 必要檔案清單

1. **tse_company_list.csv** (上市公司清單)
   ```csv
   1101,台泥,水泥工業
   2330,台積電,半導體業
   2454,聯發科,半導體業
   ```

2. **otc_company_list.csv** (上櫃公司清單)
   ```csv
   5274,信驊,半導體業
   6510,精測,半導體業
   ```

3. **TSE_buy_ranking.txt** (上市買超排行)
   ```
   #,代碼,名稱,買超張數
   1,2330,台積電,12345
   2,2454,聯發科,6789
   ```

4. **OTC_buy_ranking.txt** (上櫃買超排行)
   ```
   #,代碼,名稱,買超張數
   1,5274,信驊,5678
   2,6510,精測,4321
   ```

### 如何取得這些資料?

- **公司清單**: 從 [證交所](https://www.twse.com.tw) 或 [櫃買中心](https://www.tpex.org.tw) 下載
- **買超排行**: 從證交所「三大法人買賣超統計」取得

## 步驟 4: 啟用 GitHub Pages

1. 前往 Repository Settings (設定)
2. 左側選單找到 "Pages"
3. Source 設定:
   - 選擇 "Deploy from a branch"
   - Branch: `main`
   - Folder: `/ (root)`
4. 點擊 "Save"
5. 等待幾分鐘後,會顯示網站網址: `https://你的帳號.github.io/taiwan-stock-tracker/`

## 步驟 5: 設定 GitHub Actions 權限

1. 前往 Repository Settings
2. 左側選單找到 "Actions" → "General"
3. 找到 "Workflow permissions"
4. 選擇 "Read and write permissions"
5. 勾選 "Allow GitHub Actions to create and approve pull requests"
6. 點擊 "Save"

## 步驟 6: 測試自動執行

### 手動觸發測試

1. 前往 Repository 的 "Actions" 頁籤
2. 左側選擇 "Taiwan Stock Analysis"
3. 點擊右上角 "Run workflow"
4. 選擇 branch (main)
5. 點擊 "Run workflow"
6. 等待執行完成 (約 3-5 分鐘)

### 檢查執行結果

1. 點擊執行的 workflow
2. 查看每個步驟的執行狀態
3. 確認 `StockInfo` 資料夾中產生了:
   - `TSE_hotstock_data.json`
   - `OTC_hotstock_data.json`
   - `twstock_news.json`

## 步驟 7: 訪問網站

打開瀏覽器,訪問:
```
https://你的帳號.github.io/taiwan-stock-tracker/
```

應該會看到股票追蹤系統的網頁介面!

## 自動執行時間表

系統會在以下時間自動執行 (台灣時間):
- ✅ 週日: 09:00, 10:00, 11:00, 12:00
- ✅ 週一: 09:00, 10:00, 11:00, 12:00
- ✅ 週二: 09:00, 10:00, 11:00, 12:00
- ✅ 週三: 09:00, 10:00, 11:00, 12:00
- ✅ 週四: 09:00, 10:00, 11:00, 12:00
- ✅ 週五: 09:00, 10:00, 11:00, 12:00
- ❌ 週六: 休息

## 常見問題排除

### Q1: GitHub Actions 執行失敗?

**檢查清單:**
- ✅ 確認 `StockInfo` 資料夾中有所有必要的 CSV 和 TXT 檔案
- ✅ 確認檔案編碼是 UTF-8
- ✅ 確認 GitHub Actions 有寫入權限
- ✅ 查看 Actions 的錯誤訊息

### Q2: 網頁顯示「載入失敗」?

**解決方法:**
- ✅ 確認 GitHub Pages 已正確啟用
- ✅ 確認 Actions 至少成功執行過一次
- ✅ 確認 `StockInfo` 資料夾中有 JSON 檔案
- ✅ 清除瀏覽器快取後重新整理

### Q3: 想更改執行時間?

**修改步驟:**
1. 編輯 `.github/workflows/stock_analysis.yml`
2. 修改 cron 時間 (注意: GitHub 使用 UTC 時間,台灣時間需 -8)
   ```yaml
   # 台灣時間 09:00 = UTC 01:00
   - cron: '0 1 * * 0-5'
   ```
3. 提交更改

### Q4: 只想執行上市或上櫃?

**修改步驟:**
1. 編輯 `.github/workflows/stock_analysis.yml`
2. 找到 `env:` 區段
3. 修改 `PROCESS_MODE`:
   - `TSE`: 只處理上市
   - `OTC`: 只處理上櫃
   - `BOTH`: 兩者都處理 (預設)

## 進階設定

### 增加執行頻率

如果想在更多時間點執行,可以在 workflow 中新增更多 cron:

```yaml
on:
  schedule:
    - cron: '0 1 * * 0-5'   # 09:00
    - cron: '0 2 * * 0-5'   # 10:00
    - cron: '0 3 * * 0-5'   # 11:00
    - cron: '0 4 * * 0-5'   # 12:00
    - cron: '30 5 * * 0-5'  # 13:30 收盤
```

### 自訂爬蟲延遲

在 `stock_analysis.py` 中找到 `fetch_stocks_price` 函數,修改 `delay` 參數:

```python
tse_stock_data = fetch_stocks_price(sorted_tse, delay=2.0)  # 2 秒延遲
```

## 維護建議

1. **定期更新買超資料**: 建議每天手動更新買超排行檔案
2. **監控執行狀態**: 定期檢查 Actions 是否正常執行
3. **備份重要資料**: 定期下載生成的 JSON 檔案作為備份
4. **關注 API 變化**: Yahoo 股市可能會更改網頁結構,需要相應調整爬蟲

## 支援與協助

如遇到問題,請檢查:
1. [GitHub Actions 文件](https://docs.github.com/actions)
2. [GitHub Pages 文件](https://docs.github.com/pages)
3. Repository 的 Issues 區

---

祝你使用愉快! 📈🚀
